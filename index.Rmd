---
output: html_document
---


<style>
    body .main-container {
        max-width: 100%;
        max-height: 600px;
        padding: 0;
        margin: 0;
    }
</style>

```{r setup, include=FALSE}
knitr::opts_chunk$set(echo = FALSE)
```

```{r, echo=FALSE, message=F, warning=F, results = 'hide'}
library("leaflet")
library("rgdal")
library("ggmap")
library("raster")
```


```{r echo=FALSE, warning=F}
map <- leaflet(width = "100%")  %>% 
  addProviderTiles("Esri", options = tileOptions(minZoom=3, maxZoom=8)) %>% 
  addMiniMap(tiles = providers$Esri, toggleDisplay = TRUE) %>% 
  addScaleBar() %>%
  addSimpleGraticule(showOriginLabel = TRUE) %>%
  setView(lng = 82.5, lat = 21, 4)
map
```

##What do we map today?